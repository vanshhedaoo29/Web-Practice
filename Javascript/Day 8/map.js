var arr = [1,2,3,4];

var ans = arr.map(function(value)
{
    return 10;
})

// ans = [10,10,10,10]

/* If return 1
then ans = [1,1,1,1] */

// Map creates a new array and inserts elements which are returning inside the map
//  Naye array me returned values daalo